# 远程桌面键盘运行时验收记录

> 日期：2026-07-20
>
> 实现基线：`2419dcbfa9c035335888bd345983452d1b588dba`（`fix(host): omit released modifier from keyup flags`）
>
> 记录类型：Task12 运行时验收预置记录，不是运行通过结论。

## 1. 结论与范围

自动化回归已在最终提交基线完成；真实浏览器、真实 Host 和 macOS Quartz 的运行时验收尚未执行。本记录为后续验收保留统一案例、证据字段和判定口径。

| 状态 | 数量 |
|---|---:|
| 已通过 | 0 |
| 未通过 | 0 |
| 未执行 | 13 |

## 2. 自动化证据

| 范围 | 命令 | 结果 | 备注 |
|---|---|---:|---|
| Signal | `cd signal-server && node --test` | 128/128 通过 | 最终自动化证据 |
| Viewer、CSS、脚本 | `node --test web-client/js/*.test.js web-client/css/*.test.js scripts/*.test.js` | 250/250 通过 | 最终自动化证据 |
| Host、service | `PYTHONPATH=python-host python3 -m pytest -q python-host scripts/test_wrd_entry_health.py skills/webremote-service/scripts/wrd_service_test.py` | 105/105 通过 | 仅有既有 `mss` API deprecation warning |

上述结果只证明受测自动化合约通过；不替代真实输入链路、浏览器保留快捷键和实体键盘的运行证据。

## 3. 运行环境与前置条件

| 项目 | 记录 |
|---|---|
| 服务状态 | 2026-07-20 已通过本地受控重启加载当前工作树；`/health` 与 `/api/status` 均成功，Host 在线。 |
| Viewer 浏览器与版本 | 未执行 |
| Viewer 操作系统 | 未执行 |
| Host 操作系统与版本 | 未执行 |
| 连接方式 | 未执行（直连或 tunnel） |
| 直连候选链路 | 未执行 |
| ISO、JIS、Numpad 实体硬件 | 未执行 |

本地直连的普通浏览器已产生 Host 接收/执行 v2 key 与 reset 的脱敏运行摘要，且接管期间旧控制者输入被拒绝。该脚本未留下可复核的全案例最终快照，不能替代下表的逐项验收。公网入口在本次受控重启前后均保持原值但不可交付，因此 tunnel 路径不计为通过；本记录不写入访问地址或认证信息。

## 4. 运行时案例

所有案例的“实际结果”和“运行证据”必须在普通浏览器会话及真实 Host 上补录。记录只保留状态摘要、控制纪元、序号、重置原因、屏障往返时间、已按下数量、修饰状态与可见结果；不得记录原始输入内容或认证标识。

| 编号 | 验收目标 | 预期 | 自动化证据 | 实际结果 | 运行证据 | 状态 |
|---|---|---|---|---|---|---|
| K-01 | Windows 模式修饰归一化 | 归一化后组合行为与目标平台一致 | Signal 128/128；Viewer、CSS、脚本 250/250 | 未执行 | 未执行 | 未执行 |
| K-02 | 弹窗、输入框与 IME 切换后的释放 | 已跟踪状态可可靠归零 | Viewer、CSS、脚本 250/250 | 未执行 | 未执行 | 未执行 |
| K-03 | 跨传输顺序与重置屏障 | 切换前完成确认，后续输入不越过屏障 | Signal 128/128；Host、service 105/105 | 未执行 | 未执行 | 未执行 |
| K-04 | 操作栏组合操作 | 操作作为串行原子批次完成，生命周期中断后归零 | Viewer、CSS、脚本 250/250 | 未执行 | 未执行 | 未执行 |
| K-05 | 左右同类修饰状态 | 释放一侧不影响仍按下的另一侧 | Host、service 105/105 | 未执行 | 未执行 | 未执行 |
| K-06 | 合法长按与控制丢失恢复 | 合法长按不被误释放；控制失效立即清理 | Signal 128/128；Host、service 105/105 | 未执行 | 未执行 | 未执行 |
| K-07 | 直连与 tunnel 的双 Viewer 控制 | 单一写入者；接管按确认顺序完成；旧控制者事件无效 | Signal 128/128；Host、service 105/105 | 未执行 | 未执行 | 未执行 |
| K-08 | Host 串行转换 | 连接变化与输入执行不产生遗留按下状态 | Host、service 105/105 | 未执行 | 未执行 | 未执行 |
| K-09 | 物理组合与 Unicode 输入分流 | 物理路径与文本路径按契约隔离 | Viewer、CSS、脚本 250/250；Host、service 105/105 | 未执行 | 未执行 | 未执行 |
| K-10 | ISO、JIS、Numpad 实体映射 | 有对应硬件时验证可见结果；无硬件时诚实记录缺失 | Host、service 105/105 | 未执行：缺少对应实体键盘或尚未确认硬件 | 未执行 | 未执行 |
| K-11 | 修饰自身事件标志 | 自身事件不带自身派生状态，主键状态正确 | Viewer、CSS、脚本 250/250；Host、service 105/105 | 未执行 | 未执行 | 未执行 |
| K-12 | 生命周期故障注入 | 页面隐藏、断连、刷新与重连均经重置屏障恢复 | Signal 128/128；Viewer、CSS、脚本 250/250 | 未执行 | 未执行 | 未执行 |
| K-13 | Host 全局输入环境副作用 | 启动与停止不隐式改变用户输入环境 | Host、service 105/105 | 未执行 | 未执行 | 未执行 |

## 5. 待执行运行路径

| 路径 | 覆盖案例 | 当前状态 |
|---|---|---|
| 单 Viewer 直连 | K-01、K-02、K-04、K-05、K-06、K-08、K-09、K-11、K-13 | 未执行 |
| 单 Viewer tunnel | K-01、K-02、K-03、K-04、K-06、K-09、K-11、K-12 | 未执行 |
| 双 Viewer 直连与 tunnel 交叉接管 | K-03、K-06、K-07、K-08、K-12 | 未执行 |
| 故障注入与生命周期变化 | K-02、K-03、K-04、K-06、K-07、K-08、K-12 | 未执行 |
| ISO、JIS、Numpad 实体硬件 | K-10 | 未执行 |

## 6. 剩余限制

- 浏览器或操作系统保留的快捷方式可能无法被网页捕获；这类限制不应以模拟结果代替真实运行结论。
- ISO、JIS、Numpad 的运行时结论依赖对应实体硬件。缺少硬件时必须保留未执行状态，并注明自动化映射表已通过。
- 在服务未加载最终提交前，不得将本记录中的任一案例标为运行通过。
- 本次本地服务已加载当前工作树，但完整直连双 Viewer 收尾快照、tunnel、系统保留快捷键和实体 ISO/JIS/Numpad 仍缺失；对应案例继续保持未执行。

## 7. 补录规则

单项仅在自动化证据与对应真实运行案例均通过后，才可改为“已通过”。运行失败应记录脱敏后的状态摘要和可见结果，并将状态改为“未通过”；未进行或环境不满足的案例保持“未执行”。
